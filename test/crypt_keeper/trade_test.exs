defmodule TradeTest do
  use ExUnit.Case, async: true
  alias CryptKeeper.{Trade, Product}

  # describe "new/1" do
  #   test "with valid fields" do
  #     trade = Trade.new(product: Product.new("coinbase", "BTC-USD"))

  #     assert trade == %Trade{}
  #   end
  # end
end
